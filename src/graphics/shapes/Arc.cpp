#include "Arc.h"

namespace phantom{
	Arc::Arc(float x, float y, float radius, float start, float end){
		this->x = x;
		this->y = y;
		this->radius = radius;
		this->start = start;
		this->end = end;
	}
} /* namespace Phantom */ 